let nomeAluno; // undefined - não aponta para local nenhum na memoria 
const sobrenomeAluno = null; // valor nulo -> 
const aprovado = false; // boolean = true or false (valor logico)
console.log(typeof aprovado, aprovado);

let a1 = [1, 2]; // constante B é = constante a [1, 2], ou seja, b definido para valor de A; 
// sinal de = é um operador de atribuição.
const b2 = a;    
console.log(a, b);
console.log(typeof a, b);

let a = 2;
const b = a;
console.log(a, b); // 2, 2 

a = 3;
console.log(a, b); // 3, 2

// string, number undefined, null, boolean, symbol = dados primitivos.


